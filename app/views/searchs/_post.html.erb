<table>
    <thead>
        <tr>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
    </thead>
    <tbody id="post_image">
    <% post_images.each do |post_image| %>
      <tr>
        <td>
          <%= link_to user_path(post_image.user.id) do %>
            <%= attachment_image_tag post_image.user, :profile_image, format: 'jpeg', fallback: "no_image.png", size: "60x60" %>
          <% end %>
        </td>
        <td><%= link_to post_image.title, post_image_path(post_image.id) %></td>
        <td><%= post_image.tag_name %></td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
    </tbody>
</table>